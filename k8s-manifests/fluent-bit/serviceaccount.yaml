apiVersion: v1
kind: ServiceAccount
metadata:
  name: fluent-bit
  namespace: logging
  # MIGRATED FROM IRSA TO POD IDENTITY (Demo)
  #
  # BEFORE (IRSA):
  #   annotations:
  #     eks.amazonaws.com/role-arn: arn:aws:iam::865286230013:role/concur-test-fluent-bit-role
  #
  # AFTER (Pod Identity):
  #   - No annotation needed!
  #   - Association managed by Terraform: aws_eks_pod_identity_association.fluent_bit
  #   - Simpler setup: No OIDC conditions, no annotations
  #   - Visible in AWS Console: EKS → Cluster → Access → Pod Identity associations
